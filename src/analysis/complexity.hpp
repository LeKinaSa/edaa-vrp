
#ifndef COMPLEXITY_H
#define COMPLEXITY_H

#include "../types.hpp"

void kdTreeQuadtreeComplexityAnalysis(u32 seed = 0, bool writeToFile = false);
void quadtreeRealDataComplexityAnalysis(u32 seed = 0);
void heapComplexityAnalysis(u32 seed = 0, bool writeToFile = false);

#endif // COMPLEXITY_H
